[pytest]
testpaths = backend
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
norecursedirs = .venv
addopts = -v --strict-markers --ignore=backend/.venv
markers =
    unit: unit tests
    integration: integration tests
